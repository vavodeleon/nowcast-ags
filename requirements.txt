# Mínimos deliberadamente bajos.
#
# El código no usa nada moderno de estas librerías: FFT, lstsq, ndimage y
# lectura de HDF5 son API que llevan una década estables. Pedir versiones
# recientes solo lograría que en un Raspberry Pi de 32 bits no hubiera
# paquete precompilado y pip intentara compilar scipy desde cero: horas de
# CPU y, con 1 GB de RAM, un fallo por falta de memoria.
#
# Con estos mínimos hay versión lista para descargar en prácticamente
# cualquier máquina, incluidos Raspberry Pi OS de 32 y 64 bits.
numpy>=1.21
scipy>=1.7
Pillow>=8.0
requests>=2.25
h5py>=3.1
